# Debug TypeScript backend issue

Read:

- docs/plan.md
- .github/copilot-instructions.md
- backend/package.json
- backend/tsconfig.json
- backend/src/index.ts

We are working only on the backend.

Task:
Diagnose the current TypeScript / Node / Express error and propose the smallest clean fix.

Before editing files:

1. Explain the root cause in plain English
2. List the files you would change
3. Show the exact fix
4. State any assumptions

Rules:

- Prefer minimal changes
- Do not rewrite working code unnecessarily
- Keep TypeScript setup simple for a learning project
- If the issue is configuration-related, explain which setting is causing it

After approval:

- Apply only the agreed fix
- Explain how to rerun and verify
- Suggest a commit message
